matplotlib==3.8.0
numpy<2.0.0
pandas==2.2.2
Pillow==10.3.0
requests==2.32.3
scipy==1.14.0
sentence_transformers==2.6.1
tiktoken==0.7.0
#  --extra-index-url https://download.pytorch.org/whl/cu118
# --extra-index-url https://download.pytorch.org/whl/cu121
torch==2.8.0
torchvision==0.17.1
torchaudio==2.2.1
tqdm==4.66.2
transformers==4.51.1
umap==0.1.1
# NOTE: closedsource.py / run_merged_benchmark.py use the modern, client-based
# OpenAI SDK (openai.OpenAI(...).chat.completions.create), which requires >=1.0.
# The old 0.28.x ChatCompletion.create()-style API is NOT compatible with this code.
openai>=1.30.0
seaborn~=0.13.2
replicate~=0.31.0
accelerate>=1.1.0
# Gemini support uses the new unified `google-genai` SDK (`from google import genai`,
# `genai.Client(...)`), NOT the legacy `google-generativeai` package. Do not install
# `google-generativeai` in place of this — the two have incompatible APIs.
google-genai>=0.3.0
anthropic==0.63.0
ipykernel==6.29.5
papermill==2.6.0
bitsandbytes
triton
num2words
einops
timm
python-dotenv==1.0.0       # helpful for .env loading (optional)
typing-extensions>=4.5.0
# Optional: closed-source uncertainty via lm-polygraph (OPTICS-CMP --compute_uncertainty)
# lm-polygraph